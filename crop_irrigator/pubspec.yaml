name: saswat_agro
description: Smart Farming App

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.2.0
  flutter_tts: ^3.8.5
  speech_to_text: ^6.6.0
  geolocator: ^10.0.0
  fl_chart: ^1.2.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true